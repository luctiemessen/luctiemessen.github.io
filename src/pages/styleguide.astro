---
import MainLayout from "../layouts/MainLayout.astro";
import * as styleguide from './sg.md';

---

<MainLayout title="Styleguide" description="Styleguide for reference of all components" set:html={styleguide.compiledContent()}>
    <!-- <div set:html={styleguide.compiledContent()} /> -->
</MainLayout>